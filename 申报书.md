# OSC2026 参赛申报书

## 1. 项目基本信息
- **项目名称**：`moon-bayes-kit` —— MoonBit 可组合贝叶斯在线更新库 (MoonBit Composable Bayesian Online Update Library)
- **开源许可证**：Apache-2.0
- **参赛者/唯一贡献者**：tyytyy68 (GitHub / GitLink 账号相同)

### 代码仓库链接
1. **GitHub 代码仓库**：[tyytyy68/moon-bayes-kit](https://github.com/tyytyy68/moon-bayes-kit)
2. **GitLink 代码仓库**：[tyytyy68/moon-bayes-kit](https://gitlink.org.cn/tyytyy68/moon-bayes-kit)

---

## 2. 项目背景与选题意义
随着边缘计算与实时数据流分析的需求激增，对于概率模型进行轻量级、在线的增量更新（Online Updating）变得尤为重要。本项目 `moon-bayes-kit` 旨在利用 MoonBit 语言的高性能、类型安全及跨平台特性，构建一个轻量且模块化的**共轭贝叶斯模型（Conjugate Bayesian Models）库**。

通过利用共轭先验的数学特性，该库使得后验推断能够通过封闭形式（Closed-form）进行直接的算术增量计算，无需复杂的 MCMC 采样过程，极大地提升了模型在 WebAssembly 及原生端的推理速度。这与 MoonBit 主打的“极速运行与极简语法”特性天然契合，并且补足了目前 MoonBit 生态中轻量级概率统计/机器学习组件库的空白。

---

## 3. 核心功能与技术实现
本项目采用 MoonBit 原生语法进行实现，完全符合 0.10.3 工具链标准，并保证编译时无任何警告（`--deny-warn` 级别）。目前第一阶段包含以下核心组件：

### 3.1 Beta-Binomial 共轭模型
- 适用于**二项分布数据（如 A/B 测试、点击率预测等）**。
- 提供 `BetaBinomial::new(alpha, beta)` 进行先验初始化。
- `update(successes, trials)` 提供对先验知识的快速吸收。
- 提供后验期望（Posterior Mean）、后验方差（Posterior Variance）以及 95% 可信区间估计，帮助快速决策。

### 3.2 Gamma-Poisson 共轭模型
- 适用于**计数数据建模（如网站日访问量、事件发生频率等）**。
- 允许通过新观测的数据流，持续修正对事件发生率（Rate）的信念。

### 3.3 Normal-Normal 共轭模型
- 适用于**连续型数据观测（如传感器度量、温度、延迟等）**的均值推断。
- 采用已知观测方差的模型设定，完成均值的持续学习与在线跟进。

### 3.4 原生生态结合
- 全面兼容 MoonBit 的 `derive(ToJson, FromJson, Debug, Eq)`。
- 通过自动推导能力，模型的所有先验、后验状态可以直接持久化为 JSON 字符串并进行恢复，天然支持跨平台存储或传输（如服务端将数据传递给 Wasm 前端直接反序列化）。

---

## 4. 质量保证与测试体系
为了确保高质量的代码工程实践，本项目在开发过程中严守了以下标准：
1. **多级测试覆盖**：使用 MoonBit 原生的内置白盒测试系统 (`wbtest`)。为每个独立的模型库配备了快照测试（Snapshot Testing，利用 `inspect` 及 `json_inspect`），严格控制数值计算的精度断言。
2. **规范提交与历史回溯**：采用细粒度且具有实际意义的 Git Commit 构建项目历史，有效提交数超过 10 次，所有历史提交的 `Author` 和 `Committer` 均严格确保为 `tyytyy68` 账户本人。
3. **自动化流水线 (CI/CD)**：借助 GitHub Actions 构建了自动化的构建和验证流水线（`.github/workflows/test.yml`），每一次提交及 Pull Request 都将触发完整的格式检查 (`moon fmt`)、信息校验 (`moon info`) 及其全套测试套件 (`moon test`)。

## 5. 总结与展望
`moon-bayes-kit` 作为一个独立的数值统计算法扩展，具有极强的**通用性与可扩展性**。未来可在此基础上：
1. 扩展更多分布族（如 Dirichlet-Multinomial）。
2. 提供与 MoonBit 画图/图表库（如果生态完备）的联动接口进行概率密度曲线的绘制。
3. 拓展至简单的时间序列及变点检测（Change Point Detection）任务中。

本项目完全遵守 MoonBit 开源生态的开发规范，是丰富且推动其实用化计算生态落地的一环。
