# MoonBit 八月黑客松项目结项说明

- **项目名称**：`moon-bayes-kit` —— MoonBit 可组合贝叶斯在线更新库
- **参赛者/唯一贡献者**：tyytyy68
- **许可证**：Apache-2.0
- **GitHub**：https://github.com/tyytyy68/moon-bayes-kit

## 项目简介

本项目面向 A/B 测试、传感器校准、计数监控、分类分析和边缘推断场景，提供无隐藏状态的 MoonBit 贝叶斯更新与在线统计能力。所有模型通过不可变 `update` 产生后验，便于事件重放、检查点持久化和跨后端运行。

## 已交付能力

- Beta-Binomial、Gamma-Poisson、Normal-Normal、Dirichlet-Multinomial 四类共轭模型；
- 预测概率、预测分布、分位点、可信区间和 JSON 持久化；
- `UpdateLedger` 流式账本、Welford 在线统计、滚动窗口、EWMA/CUSUM 漂移检测；
- DenseMatrix 与贝叶斯线性回归，支持充分统计量批量更新；
- Brier、log loss、MAE、RMSE、校准误差、混淆矩阵和回归报告；
- 确定性随机源、Beta bandit、时间序列预测和 walk-forward 验证；
- 可执行基准 `cmd/benchmark` 与边界/回归测试。

## 工程质量

项目使用最新 stable MoonBit 工具链，CI 覆盖 Linux、macOS、Windows，以及 `moon check --target all --deny-warn`、`moon test --target all --deny-warn`、格式检查和 API 接口稳定性检查。项目已发布到 `tyytyy68/moon-bayes-kit@0.1.0`，发布前执行了打包校验和 MoonBit 检查。

## 原创与后续计划

本项目为原创 MoonBit 实现，不复制第三方源码、不包含外部测试数据或生成的第三方代码。后续可继续增加更多共轭分布、后验采样器和应用示例。
